export default function Loading() {
  return (
    <div className="flex flex-col p-7 text-center w-full">
      <h1 className="text-2xl font-semibold">Loading...</h1>
      <p className="text-sm text-gray-500">Hopefully not for too long :)</p>
    </div>
  );
}
